/** Paso de adelanto/retroceso, en segundos. */
export const SEEK_STEP_SECONDS = 10;

/** Mantener ◀ / ▶: segundos que avanza (o retrocede) la canción por segundo. */
export const SCRUB_SECONDS_PER_SECOND = 20;
